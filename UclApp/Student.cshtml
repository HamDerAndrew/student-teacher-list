@using UclLib.Object;
@using UclLib.Data;
@{
    Layout = "~/MasterLayout.cshtml";
    var students = GetData.GetStudents();
}
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="list-group">
                @foreach (var student in students)
                {
                    //create a unique ID for each student in the Modal
                    var modalID = "stud" + student.CPR;

                    <a href="#" data-toggle="modal" data-target="#@modalID" class="list-group-item list-group-item-action active">
                        @student.Name
                    </a>
                    <div class="modal fade" id="@modalID" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                        <div class="modal-dialog" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="exampleModalLabel">Info about @student.Name</h5>
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                </div>
                                <div class="modal-body">
                                    @student.CPR <br />
                                    @student.Phone <br />
                                    @student.Email
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                    <button type="button" class="btn btn-primary">Save changes</button>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>



